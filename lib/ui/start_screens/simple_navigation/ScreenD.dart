import '../../../utils/exports.dart';

@RoutePage(name: "ScreenRoute")
class Screend extends StatelessWidget {
  const Screend({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("ScreeD"),
      ),
    );
  }
}
